# fcg itself is deliberately NOT listed. Deploys get it from the sys.path
# shim in app/main.py; an `-e .` here made Community Cloud bake a stale
# snapshot into its cached venv that shadowed the live src/ tree. Local
# dev installs it once with `pip install -e .` (docs/HANDOFF.md).
streamlit>=1.59
pandas>=3.0
numpy>=2.5
Faker>=37.0
pytest>=8.0
supabase>=2.31
